---
import Badge from './Badge.astro'

type Props = {
  eventId: string
  category: string
}

const event = Astro.props as Props
---

<Badge id={`category-${event.eventId}`} variant="default">
  {event.category}
</Badge>

